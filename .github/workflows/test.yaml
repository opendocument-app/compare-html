name: test

on:
  pull_request:
  push:
    branches:
    - main

jobs:
  pre-commit:
    name: test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-python@v6
      with:
        python-version: "3.x"
    - run: pip install .
    - run: pip install pytest
    - run: pytest -vs
